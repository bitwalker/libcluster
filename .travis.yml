sudo: false
language: elixir
elixir:
  - 1.8
  - 1.9
otp_release:
  - 21.3
  - 22.1

script:
  - "mix format --check-formatted --dry-run"
  - "MIX_ENV=test mix do deps.get, test"

notifications:
  recipients:
    - paulschoenfelder@gmail.com
